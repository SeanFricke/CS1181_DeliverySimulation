package Objects;

/**
 * Drone variable holder class. Should really not have been a thing, but it feels right, since Truck configs are in its
 * TruckEvent object like this.
 */
public class Drone {
    // Change these to affect drone behavior
    public static final int capacity = 1, speed = 500, interval = 3;
}
